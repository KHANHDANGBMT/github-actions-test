name: Shell command

on: [push]

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello, world!"
      - name: Multiple scripts
        run: |
          node -v
          npm -v